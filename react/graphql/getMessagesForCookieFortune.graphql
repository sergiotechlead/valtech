query getMessagesForCookieFortune {
  documents(acronym: "CF", page: 1, pageSize: 1000, fields:["CookieFortune"]) @context(provider: "vtex.store-graphql"){
    fields {
      key
      value
    }
  }
}

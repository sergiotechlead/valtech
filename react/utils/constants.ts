export const PAGE_SIZE = Math.floor(Math.random() * (500 - 300 + 1)) + 300;
export const INITIAL_PAGE = 1
export const MAX_PAGE_SIZE = 5
export const MIN_TABLE_SIZE = 5

export const DEFAULT_ORDER_FORM_ID = 'default-order-form'
